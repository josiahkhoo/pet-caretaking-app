const Pool = require("pg").Pool;

// To be modified
const pool = new Pool({
    // user: "calvin",
    // password: "",
    // host: "localhost",
    // port: 5432,
    // database: "db"
})

module.exports = pool;